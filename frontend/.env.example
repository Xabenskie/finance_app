# Базовый URL backend API. Если не задан — фронт ходит по относительному /api/
# (это удобно когда есть nginx-проксирование).
# VITE_API_URL=http://localhost:8000/api/

# Куда vite dev-server проксирует /api в режиме разработки
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
